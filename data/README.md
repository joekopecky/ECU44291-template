The calibration data for the engine (Irish GDP and GNI\*, the capital-output ratio, the old-age dependency ratio projections, the state pension parameters, and the Penn World Table capital share and depreciation) arrive here with M3 in Week 8, as CSV files with their sources and the script that fetched them. Any series your extension needs beyond these, you fetch yourself and document in the same way: source, table code, date pulled, and the query.
